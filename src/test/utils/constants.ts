import * as path from 'path';

export const STATIC_FILE_PATH = path.join(__dirname, '..', '..', '..', 'src', 'test', 'staticFiles');
export const STATIC_WORKSPACE_PATH = path.join(STATIC_FILE_PATH, 'workspace');
